[Unit]
Description=Promtail service
After=network.target

[Service]
Type=simple
User=root  # <--- ansible-admin에서 root로 변경
ExecStart=/usr/local/bin/promtail -config.file=/etc/promtail-config.yml

[Install]
WantedBy=multi-user.target
